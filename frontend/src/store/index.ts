import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

import {ttoom} from './modules/ttoom';

export default new Vuex.Store({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    ttoom,
  }
})
